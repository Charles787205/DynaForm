<% const test = [ {"name": "Manucript Ratinsdasdasdsads", "date": "2024-4-7"},
{"name": "Form 1 evaluation", "date": "2024-6-7"}, {"name": "Form for Tires",
"date": "2024-8-7"} ]; %>

<main class="mainscreen w-screen items-start inline-flex">
  <!-- <div class="flex flex-row" hx-get="/components/sidebar" hx-trigger="load" hx-swap="innerHTML"></div> -->
  <div class="relative min-h-screen w-screen pt-16">
    <div class="w-full flex justify-center">
      <div class="flex flex-col h-full w-full max-w-screen-lg pt-6 gap-2.5">
        <div class="justify-start items-center gap-2.5 inline-flex py-4">
          <div class="text-neutral-600 text-5xl font-extrabold">My Designs</div>
        </div>
        <div class="self-stretch h-px border border-neutral-400"></div>

        <% test.forEach(function(item) { console.log(item); %>
        <div
          hx-get="components/createdform"
          hx-trigger="load"
          hx-swap="outerHTML"
          hx-vals='{"name": "<%=item.name %>","date": "<%=item.date %>"}'
          class="form-field"
        ></div>
        <% }); %>
      </div>
    </div>
  </div>
</main>
